apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: nginx
spec:
  releaseName: kube-prometheus
  chart:
    spec:
      chart: kube-prometheus
      sourceRef:
        kind: HelmRepository
        name: bitnami
      version: "6.6.8"
  interval: 1h0m0s
  install:
    remediation:
      retries: 3
