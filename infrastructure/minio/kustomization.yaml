apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: minio
resources:
  - serviceaccount.yaml
  - statefulset.yaml
  - service.yaml
  - ingress.yaml
configMapGenerator:
  - name: minio-env
    env: configuration/minio.env