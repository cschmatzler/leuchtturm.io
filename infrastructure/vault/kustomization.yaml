apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - rbac.yaml
  - cr.yaml
  - hr.yaml
configMapGenerator:
  - name: vault-operator-values
    files:
      - values.yaml=vault-operator-values.yaml
configurations:
  - kustomizeconfig.yaml
