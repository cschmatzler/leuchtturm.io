apiVersion: v1
kind: Service
metadata:
  name: mastodon
spec:
  selector:
    app: mastodon
  ports:
    - name: web
      protocol: TCP
      port: 3000
      targetPort: 3000
    - name: streaming
      protocol: TCP
      port: 4000
      targetPort: 4000