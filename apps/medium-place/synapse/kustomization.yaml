apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: synapse
resources:
  - serviceaccount.yaml
  #- configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
configMapGenerator:
  - name: synapse-env
    env: synapse.env
  - name: synapse-config
    files:
      - config.hcl
configurations:
  - kustomizeconfig.yaml
